package com.stockmanager.model;

public enum DateRangeType {
    DAY,   // For today's sales
    WEEK,  // For last week's sales
    MONTH, // For last month's sales
    CUSTOM // For custom date range
}